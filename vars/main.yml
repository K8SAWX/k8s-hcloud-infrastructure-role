---
# Hetzner vars
hcloud_token: null
# The name of the private network
hcloud_private_network: null
hcloud_ssh_keys:
  - "EugeniLinux"

# The awx host ip or fqdn
awx_controller_host: "localhost" 
# The awx protocol(https or http)
awx_controller_protocol: "http"
# The awx user token
awx_controller_token: null 
# The awx username
awx_controller_username: null
# The awx password
awx_controller_password: null 
